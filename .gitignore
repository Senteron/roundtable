# Python
__pycache__/
*.py[cod]
*.so
.Python
*.egg-info/
.eggs/
build/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Virtual environments
.venv/
venv/
env/

# uv lockfile (the bundle pins via mcpb/pyproject.toml; the lockfile
# is for local reproducibility only and shouldn't be reviewed)
uv.lock

# Environment / secrets
.env
.env.*
!.env.example

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Local logs / scratch
*.log
scratch/
tmp/

# Note: dist/*.mcpb and dist/*.sha256 are intentionally NOT ignored
# — bundles ship with the repo, like Senteron.
